{
  "name": "csc-307-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev -w packages/react-frontend",
    "dev": "npm run dev -w packages/react-frontend",
    "build": "npm run build -w packages/react-frontend",
    "preview": "npm run preview -w packages/react-frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
